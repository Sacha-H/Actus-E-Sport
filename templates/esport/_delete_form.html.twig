<form method="post" action="{{ path('esport_delete', {'id': esport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ esport.id) }}">
    <button class="btn">Delete</button>
</form>
